#include "main.hpp"

int
main(void)
{
    Testing::do_all_tests();

    //auto runner = Runner {};
    //runner.init_all();
    //runner.run_one("llvm_impl_add");
    //runner.run_one("llvm_impl_fadd");

    return 0;
}
